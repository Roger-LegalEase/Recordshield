{
  "name": "RecordShield Checkr Verification",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-24-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "containerEnv": {
    "CHECKR_BASE_URL": "https://api.checkr-staging.com/v1",
    "RATE_LIMIT_ALLOW_MEMORY_FALLBACK": "true"
  },
  "remoteEnv": {
    "NPM_CONFIG_AUDIT": "false",
    "NPM_CONFIG_FUND": "false",
    "NPM_CONFIG_PROGRESS": "false"
  },
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends ca-certificates curl git jq openssl postgresql-client && sudo rm -rf /var/lib/apt/lists/*",
  "postCreateCommand": "node -v && npm -v",
  "customizations": {
    "vscode": {
      "extensions": [
        "Prisma.prisma",
        "dbaeumer.vscode-eslint",
        "GitHub.vscode-github-actions"
      ]
    }
  },
  "runArgs": [
    "--init"
  ]
}
